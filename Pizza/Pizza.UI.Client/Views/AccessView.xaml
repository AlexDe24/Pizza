<UserControl x:Class="Pizza.UI.Client.Views.AccessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pizza.UI.Client.Views"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Width="325" Height="325">
    <Grid>
        <Grid Margin="25,25,25,25" >
            <Border Name="MainBorder" BorderBrush="Black" BorderThickness="3" RenderTransformOrigin="0.5,0.5">
                <Grid Margin="5,5,5,5">
                    <StackPanel Name="LoginPanel" Grid.Column="1"  Margin="25,0,25,0" >
                        <StackPanel Name="PasswordPanel">
                            <TextBlock Text="Логин" />
                            <TextBox Text="{Binding Login, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="Пароль" />
                            <PasswordBox Name="PasswordBox"/>

                            <CheckBox Name="IsRemember" Content="Запомнить" Margin="0,10"/>

                            <Button Content="Вход">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <cal:ActionMessage MethodName="HandleLoginClick">
                                            <cal:Parameter Value="{Binding ElementName=PasswordBox}" />
                                        </cal:ActionMessage>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </Button>

                            <Button cal:Message.Attach="HandleRegistrationClick" Name="Regist" Margin="0,6,0,0" Content="Регистрация"/>
                            
                            <Button Name="Exit" Margin="0,6,0,0" Content="Выход"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
